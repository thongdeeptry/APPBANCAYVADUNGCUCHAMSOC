import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

export const CardHistory = () => {
  return (
    <View style={{padding:40}}>
      <Text style={styles.title}>CardHistory</Text>
    </View>
  );
};



const styles = StyleSheet.create({});
